# Public URL of the deployed site.
# Used to build the install command shown on the docs page and the registry item URL.
# Must be set at build time (baked into the static output by Next.js).
# No trailing slash.
NEXT_PUBLIC_URL=https://time-input.yourdomain.com
